🌦️ WeatherApp (TypeScript + React)

Небольшое погодное приложение, написанное на React + TypeScript, с аккуратной архитектурой и модульной структурой.
Приложение получает данные о погоде из API и отображает их в виде карточек, поддерживает обработку ошибок и адаптируется под мобильные устройства.

⸻

📁 Структура проекта

```src/
├── components/
│   └── ui/
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Typography.tsx
│       ├── ErrorCard.tsx
│       └── WeatherCard.tsx
│
├── store/
│   └── weatherStore.ts
│
├── types/
│   └── weather.types.ts
│
├── utils/
│   └── getWeather.ts
│
├── App.tsx
├── index.css
├── main.tsx
```

🚀 Функционал
• Получение текущей погоды по введённому городу
• Интерактивный UI
• Обработка ошибок (неверный город, проблемы API и т.д.)
• Компонентная архитектура
• React + TypeScript
• Адаптивность под мобильные устройства

⸻

🛠️ Технологии
• React 19, React hook Form
• TypeScript
• Bun / Vite
• CSS, TailwindCSS
• Zustand
• OpenWeather API

⸻

⚙️ Установка

```
git clone https://github.com/your-username/weatherApp-ts.git
cd weatherApp-ts
bun install   # или npm install / yarn install, если укажешь
```

▶️ Запуск проекта

```
bun dev
```

или если используешь npm

```
npm run dev
```

🧱 Стек компонентов
• Input — поле для ввода города
• Button — сабмит
• Typography — типографические элементы
• WeatherCard — отображение данных
• ErrorCard — вывод ошибок

! Важно

API ключ хранится в .env, поэтому при клонировании с github вам надо вставить собственный api ключ в .env под именем VITE_WEATHER_KEYKEY
